From 910278dde812715be8cdaf914d540acaedd2525a Mon Sep 17 00:00:00 2001
From: Mahesh Vaidya <mahesh.vaidya@altera.com>
Date: Sun, 24 May 2026 11:45:18 -0700
Subject: [PATCH] net: mdio: Add Altera MDIO controller driver

Add a platform driver for the Altera FPGA-based MDIO controller used
on Altera FPGA boards to talk to external PHYs over the H2F AXI bridge.

The controller has two memory-mapped registers: a control register
that latches the PHY address, device, and register fields, and a data
register that holds the value being read or written.

Upstream-Status: Pending
Signed-off-by: Mahesh Vaidya <mahesh.vaidya@altera.com>
---
 drivers/net/mdio/Kconfig       |   8 ++
 drivers/net/mdio/Makefile      |   1 +
 drivers/net/mdio/mdio-altera.c | 164 +++++++++++++++++++++++++++++++++
 3 files changed, 173 insertions(+)
 create mode 100644 drivers/net/mdio/mdio-altera.c

diff --git a/drivers/net/mdio/Kconfig b/drivers/net/mdio/Kconfig
index 44380378911b..4c8dbc720450 100644
--- a/drivers/net/mdio/Kconfig
+++ b/drivers/net/mdio/Kconfig
@@ -104,6 +104,14 @@ config MDIO_HISI_FEMAC
 	  This module provides a driver for the MDIO busses found in the
 	  Hisilicon SoC that have an Fast Ethernet MAC.
 
+config MDIO_ALTERA
+	tristate "Altera MDIO bus controller"
+	depends on HAS_IOMEM && OF_MDIO
+	select MDIO_DEVRES
+	help
+	  This module provides a driver for the MDIO busses found
+	  in the Altera SoC that have a Fast Ethernet MAC.
+
 config MDIO_I2C
 	tristate
 	depends on I2C
diff --git a/drivers/net/mdio/Makefile b/drivers/net/mdio/Makefile
index fbec636700e7..e7a6ae6bec39 100644
--- a/drivers/net/mdio/Makefile
+++ b/drivers/net/mdio/Makefile
@@ -6,6 +6,7 @@ obj-$(CONFIG_FWNODE_MDIO)	+= fwnode_mdio.o
 obj-$(CONFIG_OF_MDIO)		+= of_mdio.o
 
 obj-$(CONFIG_MDIO_AIROHA)		+= mdio-airoha.o
+obj-$(CONFIG_MDIO_ALTERA)		+= mdio-altera.o
 obj-$(CONFIG_MDIO_ASPEED)		+= mdio-aspeed.o
 obj-$(CONFIG_MDIO_BCM_IPROC)		+= mdio-bcm-iproc.o
 obj-$(CONFIG_MDIO_BCM_UNIMAC)		+= mdio-bcm-unimac.o
diff --git a/drivers/net/mdio/mdio-altera.c b/drivers/net/mdio/mdio-altera.c
new file mode 100644
index 000000000000..f4a17a611971
--- /dev/null
+++ b/drivers/net/mdio/mdio-altera.c
@@ -0,0 +1,164 @@
+// SPDX-License-Identifier: GPL-2.0
+/*
+ * Altera mdio controller driver
+ *
+ * Copyright Altera Corporation (C) 2023-2024. All rights reserved
+ *
+ * This program is free software; you can redistribute it and/or modify it under
+ * the terms of the GNU General Public License version 2 as published by the
+ * Free Software Foundation; either version 2 of the License, or (at your
+ * option) any later version.
+ */
+#include <linux/clk.h>
+#include <linux/delay.h>
+#include <linux/kernel.h>
+#include <linux/module.h>
+#include <linux/netdevice.h>
+#include <linux/of.h>
+#include <linux/of_address.h>
+#include <linux/of_mdio.h>
+#include <linux/io.h>
+#include <linux/phy.h>
+#include <linux/platform_device.h>
+
+#define DRV_NAME "mdio-altera"
+
+#define ALTR_MDIO_CTRL			0x84
+#define ALTR_MDIO_CTRL_PHYAD		GENMASK(4, 0)
+#define ALTR_MDIO_CTRL_PRTAD		GENMASK(12, 8)
+#define ALTR_MDIO_CTRL_REGAD		GENMASK(31, 16)
+
+#define ALTR_MDIO_DATA			0x80
+
+/* Per-driver counter for buses without a DT alias. */
+static atomic_t altr_mdio_unique_id = ATOMIC_INIT(0);
+
+struct altr_mdio_data {
+	void __iomem *base;
+};
+
+static void altr_mdio_configure(struct mii_bus *bus, int phyad, int devad,
+				int regad)
+{
+	struct altr_mdio_data *bp = bus->priv;
+	u32 ctrl;
+
+	/* FIELD_PREP() truncates each value to its field width. */
+	ctrl = FIELD_PREP(ALTR_MDIO_CTRL_PHYAD, devad)
+	     | FIELD_PREP(ALTR_MDIO_CTRL_REGAD, regad)
+	     | FIELD_PREP(ALTR_MDIO_CTRL_PRTAD, phyad);
+
+	iowrite32(ctrl, bp->base + ALTR_MDIO_CTRL);
+
+	usleep_range(50, 100);
+}
+
+
+static int altr_mdio_read_c45(struct mii_bus *bus, int phyaddr, int devad, int regad)
+{
+	struct altr_mdio_data *bp = bus->priv;
+	int data = 0;
+
+	altr_mdio_configure(bus, phyaddr, devad, regad);
+	data = ioread32(bp->base + ALTR_MDIO_DATA);
+
+	/*
+	 * 0xFFFF in the low 16 bits is the canonical "no PHY responding"
+	 * pattern for an open MDIO bus. Return -ENODEV so phy_device_create()
+	 * does not interpret garbage as a real device.
+	 */
+	if ((data & 0xFFFF) == 0xFFFF)
+		return -ENODEV;
+
+	return data & 0xFFFF;
+}
+
+static int altr_mdio_write_c45(struct mii_bus *bus, int phyaddr, int devad,
+			       int phyreg, u16 data)
+{
+	struct altr_mdio_data *bp = bus->priv;
+
+	altr_mdio_configure(bus, phyaddr, devad, phyreg);
+	iowrite32(data, bp->base + ALTR_MDIO_DATA);
+
+	return 0;
+}
+
+static int altr_mdio_probe(struct platform_device *pdev)
+{
+	struct device_node *np = pdev->dev.of_node;
+	struct mii_bus *bus;
+	struct altr_mdio_data *bp;
+	int id;
+	int ret;
+
+	bus = devm_mdiobus_alloc_size(&pdev->dev, sizeof(*bp));
+	if (!bus)
+		return -ENOMEM;
+
+	bus->name = "altr_mii_bus";
+	bus->read = NULL;
+	bus->read_c45 = &altr_mdio_read_c45;
+	bus->write = NULL;
+	bus->write_c45 = &altr_mdio_write_c45;
+
+/*
+ * Prefer a stable id from a DT alias (mdio0, mdio1, ...); fall
+ * back to a per-driver counter so two probes do not collide on
+ * pdev->id == -1.
+ */
+	id = of_alias_get_id(np, "mdio");
+	if (id < 0)
+		id = atomic_inc_return(&altr_mdio_unique_id) - 1;
+	snprintf(bus->id, MII_BUS_ID_SIZE, "%s-%d", DRV_NAME, id);
+
+	bus->parent = &pdev->dev;
+	bp = bus->priv;
+	bp->base = devm_platform_ioremap_resource(pdev, 0);
+
+	if (IS_ERR(bp->base))
+		return PTR_ERR(bp->base);
+
+	platform_set_drvdata(pdev, bus);
+	if (np) {
+		/* try dt phy registration */
+		ret = of_mdiobus_register(bus, np);
+
+		if (ret) {
+			dev_err(&pdev->dev, "Cannot register Altera MDIO bus!\n");
+			return ret;
+		}
+	} else {
+		dev_err(&pdev->dev, "Invalid node Altera MDIO bus!\n");
+		return -ENOMEM;
+	}
+	return 0;
+}
+
+static void altr_mdio_remove(struct platform_device *pdev)
+{
+	struct mii_bus *bus = platform_get_drvdata(pdev);
+
+	mdiobus_unregister(bus);
+}
+
+static const struct of_device_id altr_mdio_dt_ids[] = {
+	{ .compatible = "altr,altr-mdio" },
+	{},
+};
+MODULE_DEVICE_TABLE(of, altr_mdio_dt_ids);
+
+static struct platform_driver altr_mdio_driver = {
+	.probe = altr_mdio_probe,
+	.remove = altr_mdio_remove,
+	.driver = {
+		.name = DRV_NAME,
+		.of_match_table = altr_mdio_dt_ids,
+	},
+};
+
+module_platform_driver(altr_mdio_driver);
+
+MODULE_LICENSE("GPL");
+MODULE_DESCRIPTION("Altera MDIO driver");
+MODULE_AUTHOR("Altera");
-- 
2.34.1

